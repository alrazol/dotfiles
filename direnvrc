## The code below can be used in combination with `.envrc` file containing just `layout poetry` in each project folder, to automatically activate poetry envs! This replaces prefixing commands by `poetry run ...`, or replaces creating a new shell with `poetry shell`.

layout_poetry() {
  if [[ ! -f pyproject.toml ]]; then
      log_error 'No pyproject.toml found. Use `poetry new` or `poetry init` to create one first.'
      exit 2
  fi
  # create venv if it doesn't exist
  poetry run true

  export VIRTUAL_ENV=$(poetry env info --path)
  export POETRY_ACTIVE=1
  PATH_add "$VIRTUAL_ENV/bin"
}
